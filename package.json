{
  "name": "riverstone-logistics-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Internal operations dashboard for Riverstone Logistics — pulls from Xero and a Google Sheet budget.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "auth": "node scripts/auth-init.js",
    "refresh": "node scripts/refresh-data.js",
    "build": "node scripts/refresh-data.js",
    "serve": "npx --yes serve public"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}
